import { motion } from "framer-motion";

function CTASection() {

    return (
        <motion.div
            className="h-screen w-full bg-accent-mid"
        >
        </motion.div>
    );
}

export default CTASection;